# v0.7 — aiUniversity design-system refactor

Execution recipe for the next implementation session. Phone-reviewable milestones, conventional commits, `data/version.json` bumps.

Opens at `v0.6.115` (pre-refactor tag) and lands around `v0.7.0` after M9.11.

---

## Pre-flight (done in this planning session before implementation starts)

- ✅ 365 tab fully decommissioned (M8.12.4).
- ✅ `aiStacked → aiUniversity` visible rename (M8.12.5).
- ✅ Dashboard components locked as style reference.
- ✅ Defaults for 34 open questions approved (`docs/plans/v0.7-open-questions.md`).
- ✅ Schema doc written (`data/schema.md`).
- ⬜ TOC markers added to `css/style.css` + `js/app.js` (in this session).
- ⬜ `CLAUDE.md` tree diagram refreshed (in this session).
- ⬜ `--color-365-*` → `--accent-violet-*` rename (in this session).
- ⬜ `milestone-deploy` skill + optional `bump-version` hook scaffolded (in this session).
- ⬜ Tag `v0.6.115-pre-refactor` on the commit before implementation starts.

---

## Milestones (M9.1 → M9.11)

Each milestone ends with: commit (conventional prefix) → bump `data/version.json` → push → phone review.

### M9.1 — Token system foundation
One commit. Zero visible delta.

- Introduce `--base` CSS var pattern per page via `[data-section="X"] { --base: ...; }`.
- Derive `--accent-solid / border / glow / surface / ink / hover / soft / warm / cool` through `color-mix(in oklch, ...)`.
- Refactor ~40–60 hardcoded `--color-{learn,projects,tools,youtube}-*` references to the new token system. Legacy tokens stay aliased so nothing breaks while references migrate.
- Add `css/overrides.css` as the single home for intentional per-component deviations (file created even if empty).
- One-time `lessons.json` migration: auto-add `status: "authored"` to every existing lesson.
- Commit: `refactor(M9.1): introduce --base + --accent-* token system [v0.7.0-dev.1]`.
- **No phone pause** — zero visible change; continue to M9.2.

### M9.2 — Pill-radius global purge
One commit. Low-risk visible polish.

- Replace every non-circle `border-radius: 9999px` / `var(--radius-pill)` with `var(--radius-sm)` (6px).
- Delete the `--radius-pill` token definition.
- Genuine circles (avatars, spinners, dots) use explicit `border-radius: 50%` or a fixed px, never the removed token.
- Commit: `style(M9.2): purge pill radius — 6px for all pill-shaped elements [v0.7.0-dev.2]`.
- **Phone pause #1** after push.

### M9.3 — Apply Dashboard glass language to Learn/Projects/Tools pages
One commit. Big visible delta.

- Lift non-Dashboard panels to near-white tone (match Dashboard's existing `.dash-panel.glass`).
- Apply transparent-glass CTA treatment, impressed-label utility `.label-debossed`, hairline divider + glass-tile idiom to every page that has a panel-with-header.
- Create shared class `.panel-tile` (or reuse `.continue-row`) as the canonical item-row everywhere.
- Commit: `style(M9.3): panel glass + tile idiom site-wide [v0.7.0-dev.3]`.
- **Phone pause #2** after push.

### M9.4 — Learn flatten: 3-zone layout + filters
Two commits. Major UX shift.

- **M9.4a**: Rip out `Courses | Quizzes` + `Academy | Tutorials` toggles. Render **Up Next** (max 3, pinned + in-progress), **Everything else** (unified list of lessons + Academy + quiz-only entries), **Done** (collapsed `<details>`). Add filter chips (`All · Lessons · Courses · Pinned · Has quiz`) + sort `<select>` (`Recent · A–Z · Progress`).
  - Commit: `feat(M9.4a): Learn — flat 3-zone layout + filter/sort [v0.7.0-dev.4]`.
- **M9.4b**: Swipe-left gesture on cards → Mark mastered + Unpin. Completed items auto-migrate to Done. Toast-with-Undo guards destructive swipes. Mastery writes to `clhub.v1.mastery`.
  - Commit: `feat(M9.4b): Learn — swipe gestures + mastery signal [v0.7.0-dev.5]`.
- **Phone pause #3** after the pair.

### M9.5 — Projects route: kill the toggle, ship the Finder surface
Two commits.

- **M9.5a**: Extend chip router to recognize `#<chip>/<view>` sub-routes. Remove `Saved | + New project` sub-pills. Saved list becomes home state of `#projects`. Glass CTA `Start a new project →` at top routes to `#projects/new`.
  - Commit: `feat(M9.5a): Projects — kill toggle, extend chip router for sub-routes [v0.7.0-dev.6]`.
- **M9.5b**: Finder moves to full-page surface with `← Back to saved` link. Thin step indicator (`Describe · Capabilities · Path · Stack · Save`), taps jump back. Examples + forks collapse to one "Start from…" block that fades once the user types. Draft persistence extended to cover path + stack; `Resume draft` banner with `Start fresh` link.
  - Commit: `feat(M9.5b): Projects — Finder as route with step indicator + resume [v0.7.0-dev.7]`.
- **Phone pause #4** after the pair.

### M9.6 — Learn drag-and-drop + pin-to-learn modal
Two commits.

- **M9.6a**: Pointer-Events DnD between Up Next ↔ Everything else. Long-press 350ms + haptic (`navigator.vibrate(10)` on supporting browsers). Fallback `⋯` menu with `Move to Up Next` / `Remove from Up Next`.
  - Commit: `feat(M9.6a): Learn — drag-and-drop between zones [v0.7.0-dev.8]`.
- **M9.6b**: Pin-to-learn modal on Tools cards. Two fields: `What do you want to learn?` (required) + `Notes` (optional). Submit creates a draft lesson stub in `lessons.json` via a small helper (no build-script required since everything runs client-side; stubs could also persist in localStorage until the user exports them — decide during implementation).
  - Commit: `feat(M9.6b): Learn — pin-to-learn modal + draft stubs [v0.7.0-dev.9]`.
- **No phone pause** between a/b; one pause after b.

### M9.7 — Tools page redesign + mastered bucket
One commit.

- Top strip "Your stack" listing mastered tools + in-use pins (reads `clhub.v1.mastery` + the pinned-tools seed in index.html).
- Catalog gets Dashboard's tile idiom. `●ᴹ` mastery badge as title-line suffix.
- Filter + sort selectors restyled to match Learn/Projects chip idiom.
- Commit: `feat(M9.7): Tools — mastered bucket + glass idiom [v0.7.0-dev.10]`.
- **Phone pause #5** after push.

### M9.8 — Bootstrapper ghost state
One commit.

- Ghost strip at bottom of every saved project card: dashed cyan border at 0.25α, impressed `Bootstrap this project` + right-aligned `coming later`.
- Tap → toast "Bootstrapper arrives in a later milestone — your stack + pins seed it." No telemetry.
- Commit: `feat(M9.8): Projects — bootstrapper ghost placeholder [v0.7.0-dev.11]`.

### M9.9 — Modal style parity
One commit.

- All modals (lesson, quiz, project detail, YouTube, pin-to-learn) inherit `.glass-elevated` + the same hairline + impressed-label idiom.
- Commit: `style(M9.9): modal glass parity site-wide [v0.7.0-dev.12]`.

### M9.10 — Version bump to v0.7.0 + ambient re-evaluation
One commit.

- Bump `data/version.json` to `0.7.0` with `M9.10` milestone.
- Re-evaluate ambient radial overlay alphas against the new lighter panels. Adjust only if the page reads flat.
- Commit: `chore(M9.10): v0.7.0 — design-system refactor complete [v0.7.0]`.

### M9.11 — Deploy + phone sweep
No commit (unless user-requested fixes surface).

- Live site sweep on iPhone. User sends screenshots of any regression; fast surgical fixes follow the "stop after 2 failed patches" rule.

**Status:** completed live with user sending screenshots; surgical fixes flowed directly into the post-sweep polish milestones below rather than landing as separate M9.11 patches.

---

## Post-sweep activation polish (M9.12 – M9.15a) — shipped

Incremental polish on top of the v0.7.0 stamp. Each bullet is a live commit; `data/version.json` milestone + version fields track the exact cut. All rules captured in CLAUDE.md design-language (animation sequencing, transforms are exclusive, no flicker on repaint, prefer static / spatial solutions) plus the matching memory entry `feedback_animation_sequencing.md`.

- **M9.12 [v0.7.1-dev.1]** — `feat`: Dashboard panels get a scroll-center activation (IntersectionObserver toggles `.is-activated` in a vertical center band). Tile baseline bg lifts off the page tone toward white so text contrast is stronger at rest.
- **M9.13 [v0.7.1-dev.2]** — `feat`: Replace the per-tile bg animation with a fixed `html::after` vignette overlay (z-index 5, pointer-events none, warm-greige gradient dim at top/bottom, transparent through the center). Spatial effect, no animation — tuneable in isolation. Activation observer extended to `.learn-zone`. Widen band to ~50% (rootMargin -25%).
- **M9.13a [v0.7.1-dev.3]** — `style`: Band tightens to ~35% (rootMargin -32.5%). Vignette peak alpha 0.13 → 0.24 with 3-stop falloff.
- **M9.14 [v0.7.1-dev.4]** — `feat`: Unify the active-highlight recipe across nav chips + filter chips + activated panels (thin `--accent-border` + tight `--accent-glow`). Collapse all per-section `--base` overrides to a single `--base-shared` token at `:root` (temporarily `#ff7a1a` tangerine); inline `/* was #XXX */` comments preserve each original literal for later restoration.
- **M9.14a [v0.7.1-dev.5]** — `style`: Drop the inset 1px inner ring from the five unified highlight recipes so the line reads roughly half-weight. Dashboard CTAs + drag-drop drop-candidate keep their inset rings (different components, intentional punch).
- **M9.15 [v0.7.1-dev.6]** — `feat`: Activation distributes to the interactive bits inside an activated panel. Panel surfaces no longer paint accent (too much color on the big surface — explicit override of the unified recipe). Dashboard CTAs go flat/neutral at rest and only light accent border + inset ring + outer glow inside an activated panel. Dashboard icons (`.dash-panel-icon--*`) go greige `rgba(30,24,16,0.22)` stroke + `filter: none` at rest and reclaim their stroke + drop-shadow glow only in-band. Tiles inside activated panels raise up with a coordinated shadow. `--base-shared` flips from tangerine to `#84cc16` warm vibrant green. Band tightens to ~25% (rootMargin -37.5%).
- **M9.15a [v0.7.1-dev.7]** — `style`: Band tightens to ~20% (rootMargin -40%). Same model; narrower observer window.

---

## M9.16 polish suite — shipped (2026-04-20)

Second post-sweep wave. Mix of new feature (link-in-modal), deferred contrast review, audit-driven polish, and hygiene. All rules from CLAUDE.md design-language still in force.

- **M9.16a [v0.7.1-dev.8]** — `style`: Activation band's vertical center raised 8% above viewport center (rootMargin `-40%/-40%` → `-32%/-48%`). Band height unchanged at 20%.
- **M9.16b [v0.7.1-dev.9]** — `feat`: Link-in-modal pattern. Global click interceptor routes external `<a>` clicks into a sandboxed `.doc-modal` iframe (video links route to existing `.video-modal`). Close via X / backdrop / ESC (capture phase, topmost only) / phone back gesture (history.pushState + popstate). Sandbox includes `allow-same-origin` (required for JS-heavy news sites), referrerpolicy no-referrer, no `allow-top-navigation`. Prominent "Open on {hostname} ↗" escape hatch for embed-blocked sites.
- **M9.16b hotfix [v0.7.1-dev.10]** — `fix`: Doc-modal header respects `env(safe-area-inset-top)`; Open button promoted from icon-only to labeled pill.
- **M9.16c [v0.7.1-dev.11]** — `feat`: Doc-modal polish — focus trap + restore, loading spinner, shared `refreshModalOpenBodyClass()` helper replacing 7 drifted inline selector lists.
- **M9.16d [v0.7.1-dev.12]** — `feat`: Doc-modal blocked-state UI (circle-slash icon + "Open on {hostname}" button) overlays iframe when `iframe.contentDocument` probes empty after load. Generation counter protects against close→open races.
- **M9.16e [v0.7.1-dev.13]** — `style`: Vignette gradient compressed toward edges (dark band 25% → 16% of viewport each side; clear middle 50% → 68%). Peak alphas at 0%/100% unchanged.
- **M9.16f [v0.7.1-dev.14]** — `style`: Tool-modal header safe-area padding + vendor eyebrow contrast 0.35α → 0.50α.
- **M9.16g [v0.7.1-dev.15]** — `style`: Bootstrap-ghost strip dashed border 25% → 35% accent-solid, bg tint 3% → 5%.
- **M9.16h [v0.7.1-dev.16]** — `style`: Your-stack empty-state text color-mix(text-2 50%, text-3) (≈0.50α).
- **M9.16i [v0.7.1-dev.17]** — `style`: `.tool-card.glass` joined the Learn-panel near-white override; hover swapped legacy `--accent` → `--accent-border`/`--accent-glow`, dark 0.75α shadow → subtle green shadow. `.tool-card-claude` hover kept orange glow under same lighter envelope.
- **M9.16j [v0.7.1-dev.18]** — `style`: `.project-chip-claude` orange tint removed (rule kept empty as a hook for a future color choice).
- **M9.16k [v0.7.1-dev.19]** — `style`: Doc-modal detection moved out of iframe load handler (false-positive blocked flags during cross-origin navigation transitions); detection now runs only in a single 3s timer. Safe-area-inset-top added to `.storage-modal-head` + `.backup-modal-head`. Dead `--accent` compat alias removed.
- **M9.16l [v0.7.1-dev.20]** — `style`: Nine-item audit-driven polish chunk. Nav chip transitions → `--ease-premium` + elevated active shadow. Dashboard panel icons stroke 0.22α → 0.35α. `.card-pill` + `.tutorial-toggle` radii 999px → `--radius-sm`. `.card` `cardIn` animation wrapped in `prefers-reduced-motion` (final state outside). Severity pulse `currentColor` → `var(--dot)`. `.your-stack-tool` padding symmetric 7px 11px. `.tool-card:active` scale(0.99) → translateY(1px). `.save-btn-mastery:hover` added with accent-forward tint.
- **M9.16m [v0.7.1-dev.21]** — `chore`: js/app.js TOC regenerated (previous anchors drifted ~500 lines).
- **M9.16n [v0.7.1-dev.22]** — `style`: Second audit wave. `.trow-type` + `.type-legend-item` radii 999px → `--radius-sm`. Unified `:focus-visible` ring (2px `--accent-border` + 2px offset) across 15 interactive elements in tool/doc/video/yt/backup/storage/lesson/recipe modals.
- **M9.16o [v0.7.1-dev.23]** — `chore`: Session wrap hygiene. `docs/status.md` rewritten with the full M9.16 ledger; plan doc gained the "M9.16 polish suite — shipped" section + converted the deferred contrast review to the completed outcome table; CLAUDE.md tree comment updated; per-session memory files refreshed.
- **M9.16p [v0.7.1-dev.24]** — `refactor`: Drop link-in-modal. Header probe of the top 8 hosts in `data/latest.json` (platform.claude.com, github.com, anthropic.com, news.google.com, techcrunch.com, bloomberg.com, plus simonwillison.net + lovable.dev) showed ~95% set `X-Frame-Options` or CSP `frame-ancestors` to forbid embedding — the iframe preview premise wasn't viable for this feed. Removed the `#doc-modal` template (index.html), the `.doc-modal*` block + three focus-ring entries (css/style.css), and the global `<a[href]>` click interceptor + `openDocModal` / `detectDocBlocked` / popstate / focus-trap / ESC wiring (js/app.js). Kept the shared `refreshModalOpenBodyClass` helper (minus its `.doc-modal` selector). YouTube per-card handler still routes to the in-app video modal independently of the removed interceptor. External anchors now open natively via `target="_blank"`.
- **M9.16q [v0.7.1-dev.25]** — `chore`: Docs hygiene — plan doc + status.md + memory updated with M9.16o and M9.16p entries; the "known open after M9.16" note retired (the doc-modal it referenced no longer exists).

## M9.17 suite — in flight (2026-04-20)

- **M9.17.0 [v0.7.1-dev.26]** — `chore`: Scaffolded the `design-amend` skill (`.claude/skills/design-amend/SKILL.md`) for systemic rule changes (impact list → docs-first → chunked cascade). Pairs with `design-review`.
- **M9.17a [v0.7.1-dev.27]** — `fix`: `min-width: 0` applied to the flex-column card chain (`.learn-zone-body`, `.learn-item`, `.learn-item-head`, `.tool-card`, `.project-card`) after Up Next item titles bled past the card edge.
- **M9.17a.1 [v0.7.1-dev.28]** — `fix`: `:has(+ .learn-menu)` reserved the ⋯-menu right gutter on `.learn-item`. Superseded by a.4.
- **M9.17a.2 [v0.7.1-dev.29]** — `fix`: Unconditional 44px right gutter + `overflow: hidden` on `.learn-item` and `.learn-item-head` as belt-and-suspenders after the a.1 fix's `:has()` selector appeared stale on phone review. Superseded by a.4.
- **M9.17a.3 [v0.7.1-dev.30]** — `chore`: Cache-buster `?v=<version>` added to `css/style.css`, `css/overrides.css`, `js/app.js` in `index.html` so subsequent milestone-deploys force a fresh fetch instead of riding GitHub Pages' browser cache. Bump the query alongside `data/version.json` on every deploy.
- **M9.17a.4 [v0.7.1-dev.31]** — `refactor`: Simplified the learn-item constraint recipe. `.learn-item-wrap` became a flex row; `.learn-item` is `flex:1`; the ⋯ menu (now retired) was a natural flex sibling at the right. All prior padding-right / overflow-hidden hacks removed. Title's own `overflow: hidden` + `text-overflow: ellipsis` handles truncation.
- **M9.17b.a [v0.7.1-dev.32]** — `feat`: Inline pin + mastery + draft-delete buttons replacing the ⋯ collapsed menu on Learn items. Buttons are 28×28 tiles as flex siblings of the card anchor/button (no click bubble to navigation). Draft delete uses a red destructive button with two-tap confirm (arms 3s, second tap deletes). New SVG constants `MASTERY_SVG_*` + `TRASH_SVG`. Static `.learn-item-pin` / `.learn-item-mastery` dot markers removed.
- **M9.17b.b [v0.7.1-dev.33]** — `feat`: Grab handle (⋮⋮ glyph) added to the left of each non-Done learn-item wrap. Touching the handle initiates drag-to-reorder immediately — replaces the retired long-press-to-drag (no timer, no direction inference). `wireLearnRowGestures` simplified to swipe-only; new `wireLearnGrabHandle` owns drag. `LEARN_LONG_PRESS_MS` / `LEARN_LONG_PRESS_SLOP` constants retired in favor of `LEARN_GESTURE_SLOP`.
- **M9.17b.c [v0.7.1-dev.34]** — `feat`: Shrunk the tap target on Learn items. Whole-card was an anchor/button → accidental opens on phone. Now only `.learn-item-head` (kind + title + state pills) is the link; meta / summary / coverage are passive siblings. `.learn-item` container becomes plain `<div>`. Head gets 48px tap-target via 6px vertical padding. Capture-phase swipe-suppression still blocks the head-link's default.
- **M9.17c [v0.7.1-dev.35]** — `chore` (amendment): Introduced the `--danger` token family (`--danger-{solid,border,glow,surface,ink,hover}`) derived via `color-mix` from a single hue source — same pattern as `--base-shared` → `--accent-*`. Amended CLAUDE.md design-language section and the `design-review` skill's color hard-rule to mandate the token. No surface migrations yet; those land in M9.17c.a. Out of scope: sentiment / status pills where red = severity, not action.
- **M9.17c.a [v0.7.1-dev.36]** — `style`: Destructive-color cascade. Migrated `.learn-action-btn-danger` (was `#c0392b`), `.project-delete` (was `#f43f5e`), `.storage-key-delete` (was mixed `#f87171`/`#f43f5e`), and `.pin-picker-remove` (was mixed) to `--danger-*` tokens. All four now share the same red; future hue retunes are a one-line :root edit.
- **M9.17d [v0.7.1-dev.37]** — `style`: No text-selection on tappable surfaces. Added `user-select: none` + `-webkit-user-select: none` + `-webkit-touch-callout: none` to every tappable / draggable / clickable class (card rows, action buttons, grab handles, filter chips, nav chrome, CTAs). Informational text below heads (summary, meta, coverage, long-form body) stays selectable so users can copy. CLAUDE.md design-language section updated with the rule.
- **M9.17b.d [v0.7.1-dev.38]** — `refactor`: Retired swipe-left-mastery. The inline ✓ button from M9.17b.a is the discoverable mastery path, so the swipe state machine (touchstart/move/end, pointer events, direction-commit logic, `LEARN_SWIPE_*` / `LEARN_GESTURE_SLOP` constants, `handleLearnSwipeLeft`) was deleted. `wireLearnRowGestures` collapsed to just the capture-phase click-suppression that blocks a head-link click after a drag. ~120 lines removed.
- **M9.17e [v0.7.1-dev.39]** — `chore`: `js/app.js` TOC regenerated after the M9.17 wave drifted anchors ~100 lines. `docs/status.md` caught up with the M9.17 ledger.

---

## Deferred — final color / contrast review — completed (M9.16f–j)

User flagged (post-M9.9) that several spots would likely need greater contrast for readability. Ran as a dedicated sweep on 2026-04-20.

**Outcome per candidate:**
- **Snippet-recipe popover** (`.recipe-modal-card`) — **skipped**, host `#recipes` was parked with the charts in M8.11.4, modal is currently unreachable from the UI.
- **Impressed modal eyebrows** (`.label-debossed`) — only `.tool-modal-vendor` bumped (M9.16f: 0.35 → 0.50). The shared utility left at 0.35α; other debossed surfaces (storage/backup/pin-picker sub-labels, lesson-modal-min) not flagged by user.
- **Bootstrap ghost strip** (`.project-bootstrap-ghost`) — bumped (M9.16g: border 25% → 35% accent-solid, bg 3% → 5%).
- **Your stack empty-state** (`.your-stack-empty`) — bumped (M9.16h: text-3 → color-mix(text-2 50%, text-3) ≈ 0.50α).
- **Filter-chip counts** (`.filter-chip-count`) — **left as-is**, user said "good" on phone review.
- **Mastery badge** (`.tool-mastery`) — **left as-is**, user said "fine for now".
- **Project-chip claude-native tint** — **removed entirely** (M9.16j). User opted to drop the orange tint; rule kept empty as a hook for a future color choice.

If new contrast spots are raised later: don't re-run the whole list — ask for the specific spot, bump only that. No global token edits.

---

## Commit conventions

Format:
```
<type>(<milestone>): <imperative summary> [<version>]

<optional body>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Types used in this refactor: `refactor`, `style`, `feat`, `chore`. No `[skip ci]` on design commits.

---

## Phone-review pause points (explicit)

| After | Expect | Unblock signal |
|-------|--------|----------------|
| M9.2  | first visible delta (squared pills) | `continue` / `M9.3` |
| M9.3  | panels + tiles site-wide           | `continue` / fix-then-continue |
| M9.4b | Learn flat layout operational      | `continue` / fix-then-continue |
| M9.5b | Projects route + Finder flow       | `continue` / fix-then-continue |
| M9.7  | Tools page parity                  | `continue` / fix-then-continue |

Agent halts with `Pushed M9.N. Live in ~60s. Waiting.` and does not continue autonomously.

---

## Failure recovery

1. User sends screenshot + problem description.
2. Agent: `git diff HEAD~1 <affected-file>` → identify the change → ONE surgical fix.
3. If the fix doesn't land after second attempt, STOP. Reassess root cause with user input. Do not ship a third patch.

---

## Running state (`MEMORY.md` entry to add before session)

```
# v0.7 design-system refactor session state
- current_milestone: ""           # filled at session start ("M9.1" etc.)
- last_version: "0.6.115"         # bumped after each milestone commit
- phone_reviewed_through: ""      # last milestone user confirmed on phone
```
