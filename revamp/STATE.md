# Build State — aiUniversity revamp

Running ledger. Rehydrate from this after context compaction.

## Live

- Production deploy at **https://onerrorgotowtf.github.io/claudeHub/**
- Branch: `main` (merged from `claudeRevampAttempt`)
- Pages source: **GitHub Actions** (workflow: `.github/workflows/deploy-pages.yml`)
- Old feed-refresh workflow (`update.yml`) still runs every 2h in parallel; it doesn't touch `revamp/`

## Shipped (most recent on top)

- **Atom-system lab redesign — 7-chunk autonomous run (2026-04-25)** — Stood up the diagnostic infrastructure for iterating the locked atom-system design (atom-system-plan.md). **Chunk 1 — `AtomLabHud`** (src/ui/atom/AtomLabHud.{tsx,module.css}): bottom-pinned 4-line monospace HUD; line 1 (build·{commit}/route/viewport/clock) refreshes 1s, line 2 (compact config serialization) re-renders on prop change, line 3 (math) is rAF-throttled 30Hz via direct DOM textContent (NOT React state — no useFrame back-pressure), line 4 (events newest-first 5-deep) refreshes per 1s tick; hidden `data-atom-debug-context` JSON node + `console.debug` heartbeat for screenshot/remote-DevTools debugging; `pointer-events:none` + `aria-hidden`. **Chunk 2 — vite.config build-time commit injection** via `define: { 'import.meta.env.VITE_GIT_COMMIT': JSON.stringify(<7-char SHA>) }`, verified empirically (probe build → SHA literal in dist/assets/index-*.js). **Chunk 3 — `/labs/atom-states`** (single-state isolation): runtime modules under src/ui/atom/runtime/ (`types.ts` discriminated `StateConfig` union + `Vec3` + `StateContext` + `planeLift()`; `orbit.ts`/`straight.ts`/`spiral.ts`/`pulsate.ts`/`pause.ts` each implementing `positionFn`+`scaleFn`; `index.ts` exporting the registry + `evalState()` + `evalVelocityMagnitude()` + `defaultConfigFor()`); page (LabsAtomStates.{tsx,module.css}) renders fullscreen R3F canvas (frameloop=demand, camera z=5), top-right floating card with state radio (5 options), per-variant slider controls (size/aspect/revs/duration/plane/etc.), color pickers (head/halo/trail — direct material tint; full color system still deferred), Replay button, ×-collapse to 44pt icon. **Chunk 4 — `/labs/atom-transitions`** (LabsAtomTransitions.{tsx,module.css} + runtime/transitions.ts): two-state sequence; `checkComposition()` grays out illegal B options based on A (spiral.inward only after orbit; spiral.outward only after at-point); `computeWindowMs()` implements the locked `windowMs = transitionWindow · 0.5 · min(durL,durR)` formula with MIN_WINDOW_MS=40ms floor; `transitionWindow` slider (0–1, default 0.5) + A.dur/B.dur sliders; ElectronTransitionProbe runs A→B back-to-back via evalState passing aEndPos as ctx.prev; frame-to-frame finite difference of rendered position gives truthful |v|; seam sample records last-A |v| vs first-B |v|; boundary chips classify Δ|v|/peakV green<2.5%/yellow<5%/red>=5% (eye-perceptible threshold inherited from retired blend-test); auto-replay on any change. **MVP scope note:** seam is concat-only today — chips read red, which is the diagnostic working as intended; path-blend math gets iterated *on* this stage (next workstream). **Chunk 5 — retired `/labs/atom-blend-test`**: deleted LabsAtomBlend.{tsx,module.css}, removed lazy import + route. Salvaged before delete: dt floor at 1/240 (≥4ms — prevents tiny first-frame Δ from producing a 60+ unit/s velocity spike pinning peakV high) + 3-frame velWarmup (skip first 3 |v| samples), both ported into LabsAtomTransitions so chips read truthfully on first replay. **Chunk 6 — polish + reduced-motion gate**: `usePrefersReducedMotion()` hook (live-updated via matchMedia 'change' event, SSR-safe); both labs gate useFrame on the flag — when set, probe seeds trail buffer at t=1, snaps head+halo to end-state, writes one-shot math line with `extra:'reduced-motion'`, skips animation; HUD renders normally so screenshots still capture full debug context; bumped Transitions radio to 44pt; aria-hidden + 100dvh confirmed. Single-source-of-truth plan: revamp/docs/atom-system-plan.md (chunk tracker fully ✓; preserves all locked decisions for the math-iteration phase).
- **Animated atom logo + `/labs/atom` page (2026-04-24)** — New `/labs/atom` experimental route + the topbar wordmark replaced with the same animated atom. Stack addition: `three@0.184`, `@react-three/fiber@9.6`, `@react-three/drei@10.7` (Trail only), `@react-three/postprocessing@3.0` (installed but unused in final), `meshline@3.3.1`. Core rendering in a single `AtomComposition` component (exported from `src/pages/LabsAtom.tsx`) with two variants via `compact` prop — full-page on `/labs/atom`, topbar-sized inline in `AppShell`. **Scene:** 3 electrons on mutually-orthogonal elliptical orbits (XY/YZ/XZ planes, semi-major 1.40 / semi-minor 0.85, same `ORBIT_SPEED` so phase offsets 0 / 2π/3 / 4π/3 stay constant — no beating/clumping). Group rotation `[π/4, π/4, 0]` for 3/4-view dimensionality. Camera `z=11` labs / `z=5.5` compact. Per-orbit laps before settle: 3 / 5 / 6.5. **Trail:** custom parametric MeshLine (pmndrs/meshline) — 96 uniformly-spaced points sampled directly from the orbit equation every frame, alpha-ramp DataTexture (`pow(x, 2.2)`) for taper. Replaced drei `<Trail>` which alias-gapped at fast speeds because it samples per-frame position. **Settle:** electron orbits for N laps, then `orbitPosMorphed` spirals it in — orbit center slides `origin → target` while radii scale `1 → 0` over 2π t-space, easeOutCubic'd. Target = projected i-dot of "ai" via CSS-pixel-to-world math (`projectPixelToLocal` does `screen → NDC → world-at-z=0 → inverse group rotation → local`). DOM measurement via `useLayoutEffect` in TWO passes (first measure `aiHalf`, re-measure i-dot after layout re-shifts — without the split, the measurement reads the pre-shift DOM and the target lands on the U instead of the i). **Collision pulse:** 420ms asymmetric flicker keyframe (stops at 8 / 14 / 22 / 38 / 58% with brightness 1.18 / 0.82 / 1.1 / 0.95 / 1.03) on a `cubic-bezier(0.2, 0.9, 0.1, 1)` curve — research-backed neon-tube strike-and-stabilize, not symmetric ease-in-out. Electron scale bumps `1 → 1.45` + halo expands `1 → 2.6× at 0.5 opacity`. **Per-electron post-land behavior** via `postLandVisibility` (0 / 0.33 / 1): first disappears after flash, second stays dim at 33% over the i-dot, third stays fully lit — electrons accumulate visibly. **"ai" text behavior:** debossed `rgba(0,0,0,0.28)` at rest; strike snaps to full white with 5-layer glow stack (radii 3/6/12/22px — tightened from earlier 4/10/22/42/72px for readability); decays via 720ms `cubic-bezier(0.4, 0, 0.8, 0.3)` back to debossed for strikes 1-2, stays lit permanently after final strike (`allLanded` check). Glow-during-flash scales with progress. **"University"** stays dim-debossed always. **iOS Safari compositor-race fix:** research identified that a sticky header containing a WebGL canvas builds two layer-tree snapshots on initial paint, committing sticky position from stale snapshot (symptom: blank greige gap above topbar on refresh). Fix: `min-height: 100dvh` (fallback 100vh), `transform: translateZ(0); will-change: transform` on `.topbar` forces its compositor layer upfront, `min-height: 60px` so sticky box is committed before async canvas content loads. **Architecture note:** `AppShell` statically imports `AtomComposition` from `pages/LabsAtom`, which defeats the route's dynamic-import code-split — main bundle grew from ~800KB → 1.7MB (508KB gzipped). Intentional trade for always-visible animated logo. Extract to `src/ui/atomScene.tsx` later if bundle size matters. **Knobs:** `RADIUS_A`/`B`, `ORBIT_SPEED`, per-orbit `laps`/`phase`/`postLandVisibility` in `ORBITS`, `POST_SCALE`/`POST_HALO_SCALE`/`POST_HALO_OPACITY`, `PULSE_T`, `SETTLE_DURATION_T`, strike keyframe percentages + brightness values, glow-shadow radii + alphas in `buildAiStyle`. Dot-Y offset fraction `iRect.height * 0.32 - 2` is font-dependent; retune on font swap.
- **UX + data polish wave (2026-04-23, session 2)** — Big session on top of the earlier autonomous run. Highlights: **Library** multi-tag filter (`?tag=a,b,c` AND'd, clickable row chips for drill-down, Clear-all chip when 2+ filters active, Show-all-tags disclosure, kind/sort/saved all URL-backed for shareable views), **Global search** matches tags on tracks/topics/projects and remembers the last 5 queries in localStorage as tappable chips. **Dashboard** Projects CTA now reads "Jump back in · <Title>" for the most-recent in-progress project; Learn panel surfaces a flame-pulsed streak pill when streak > 0. **Me** streak flame gets a subtle 3s opacity flicker (compositor-safe — opacity only). **Projects** page splits live vs archived (Completed + Canceled collapse into a Disclosure) and gets status-chip filter row (All / In progress / Planned / Backlog, `?status=…` URL-backed). **TopicDetail** gets an "Add to plan / In plan" toggle next to the header; quiz tiles get a "Review" button to reopen the last attempt. **LibraryDetail** tags are now clickable Library-filter links; Save-for-later button added next to Pin; "See also" + "Referenced by" sections pull from `getBacklinks` + tag-overlap. **ProjectDetail** gets "Further reading" + "Similar projects" tiles. **LessonView** add-to-plan banner retired the fake modal attrs (role=region + aria-live + Esc dismiss). **Quiz review feature shipped**: new Dexie `quizAttempts` table (schema v9) persists every attempt with full answers; TopicDetail "Review" button hits `/learn/quiz/:id?review=1` which hydrates the latest attempt into the result+review screen without retaking. Per-question review body renders per kind (MCQ shows every choice marked, ordered-steps shows submitted vs correct order, code/short-answer shows submitted vs expected). Canned `explain` strings render under each question when present. "Accept score" button added then **removed** — research across Khan/Coursera/edX/Udemy/Codecademy/Brilliant/Quizlet confirmed no platform gates review on accept; retake just produces a new score. **Quiz report-flag relocated** out of the card footer into its own row below the card (Apple HIG tap-target separation from Next button); reason chips tinted `--danger-*` so flagging context reads visually distinct. **A11y sweep**: form-label associations across Settings / ProjectNew / Library / Feedback; aria-live regions on QuizView result, GlobalSearch results, Library miss-noted. **Schema fix**: `user_project_events` → `project_events` with `from_value`/`to_value` columns (reserved-word trap in supabase-schema.sql, pre-migration). **CSS token audit**: 6 CSS literals routed through tokens (ink-inverse, radius-sm, dur-*, ease-*). **Detail-page loading vs not-found** distinction on Topic / Library / Project. **`/labs/radar` experiment** (SVG radar with 6 electric-color swatches + hand-rolled confetti) was built, viewed, and reverted — left as a revert trail in git history. Background docs: `docs/research-db-migration.md` (Supabase cutover spec), `docs/research-quiz-review.md` (compare-matrix across 8 learning platforms; embedded in commit message). Memory files iterated heavily (motion policy, question-format protocol). Ship cadence: ~25 commits, all deploys green.
- **Autonomous polish — detail-page loading vs not-found + modal-shell de-dup + DB-migration research (2026-04-23)** — Three small follow-ups from `docs/next-session-plan.md`. (1) **TopicDetail / LibraryDetail / ProjectDetail** previously used `T | null` initialized to `null`, collapsing "still loading" and "id doesn't exist" into one `!item` branch that rendered a blank page. Widened state to `T | null | undefined` so `undefined` = loading (empty div, replaced on data arrival) and `null` = not-found (titled card + Back link) — mirrors the LessonView pattern. Prevents a silent crash-surface when a deleted/renamed id is opened. (2) **`ModalCardShell` helper** inside Learn.tsx de-dupes the head (title + blurb + close X) + foot (N-of-M + Add-all button) chrome shared by CategoryModalBody and ExpandedPack; only the unique body slot differs. Promote to `src/ui/` when a third dive-in modal appears. (3) **DB migration research doc** at `revamp/docs/research-db-migration.md` — authoritative spec for the eventual Supabase cutover: schema refinements tracking every post-draft chunk (Chunk H tags/edges, Chunks I/J/K/L/O feature tables), full RLS policies with `is_admin()` helper + escalation-proof `protect_is_admin` trigger, auth flow with TOTP 7-day grace, content seed via `scripts/seed-supabase.ts` reusing `src/db/seed.ts`, dual-impl `repo.dexie.ts`/`repo.supabase.ts` behind `VITE_USE_SUPABASE` flag for no-code rollback during soak, 10-step playbook, ~60–80h estimate, 8 open questions. Flags a schema bug: `user_project_events` uses reserved-word columns `"from"`/`"to"` → rename before going further.
- **Learner habits + trust (Chunk O, 2026-04-23)** — Four related additions that turn passive consumption into active study. (1) **Streak + weekly-days** card on `/me` — `currentStreak` and `daysThisWeek` in new `src/lib/studyStats.ts`; day-bucketing by local YYYY-MM-DD; streak tolerates "today-not-yet-touched" (falls back one day before resetting). (2) **Due-for-review list** on `/me` — `dueForReview(progress, staleDays=7)` returns passed-quiz topics last touched > 7 days ago, oldest-first; top 5 render as a list with direct topic links. (3) **General feedback channel** — new Dexie table `feedback` (schema v8) + `repo.logFeedback` + dedicated `/feedback` page with kind picker (bug / idea / content-issue / other) + "Thanks — logged" confirmation. Reachable from the footer alongside Colophon. (4) **Read-later queue on Library** — `LibraryItem.savedForLater` flag + `repo.toggleSavedForLater` + bookmark icon on every row (distinct from pin) + a "Saved for later only" toggle inside the Filter popover with a clear-chip alongside Kind/Tag/Sort. Spaced-repetition is intentionally crude v1 (flat 7-day threshold); proper per-topic intervals arrive with the DB migration.
- **Topic cross-links + Library tag facet + engagement prompt (Chunk L, 2026-04-23)** — Surfaces the Chunk H data layer. **TopicDetail** gains three new sections: (a) **Tags row** under header — chip per tag, each links to `/library?tag=X`; (b) **Prerequisites** — lockable prereq topics rendered before Lessons when `prereqTopicIds` is non-empty; (c) **See also** — symmetric related topics from `relatedTopicIds`; (d) **Further reading** — library items from `relatedLibraryIds`. **Library** gains a **tag facet** inside the Filter popover — top 12 tags by frequency, AND'd with Kind + Search. URL-driven: `?tag=X` sets the filter (so Topic-page tag links work), and toggling in the popover rewrites the URL. Active tag shown as a clear-chip alongside Kind/Sort clear-chips. **Engagement prompt on lesson start**: first time a user opens a lesson for a topic that isn't in their active plan and they haven't previously declined, an inline accent banner asks "Add X to your plan?" — "Add to plan" calls `repo.addPathwayItem(topicId, 'manual')`; "Not now" calls new `dismissAddPrompt(topicId)` action so it never re-asks for that topic. `UserProfile` gains `promptDismissedTopicIds[]`, persisted via zustand. Prompt uses the accent-surface treatment from the recent color sweep; slides in with a single 240ms enter animation; respects reduced-motion.
- **Onboarding retired + Settings overhaul (Chunk K, 2026-04-23)** — Onboarding flow + route gone. `/onboarding` 301s to `/`. `Onboarding.tsx` deleted (kept `.module.css` — Settings reuses its option-grid styles). App no longer first-run-redirects anywhere; users land directly on Dashboard. UserMenu drops the Pathway radio group and the Retake-onboarding link — only `My progress` + `Settings` remain. Settings loses its entire Pathway section (nothing to choose — pathway is emergent). Every remaining field gains a **"Why we ask"** transparency note naming the specific feature it unlocks (handle → menu label + future URLs; work styles → project/tool recommendations; devices → future bootstrapper; years coding → dev bootstrapper defaults; known topics → Learn dims what you know). No fields are required; skipping just leaves that feature generic. Retake-onboarding / pathway-stamp-on-settings-change logic both removed.
- **Learn refactor: Categories + Starter packs, retire MyPathway (Chunk J, 2026-04-23)** — Learn page restructured around the Chunk H Category layer. Tracks group under their Category (Foundations / Building with AI / AI at Work / Generative Media); within each category, pathway-sort still floats "For you" up and collapses "Everything else in this category" for dev. **Slim `My plan (N) →` link** replaces the old three-row pathway panel — routes to `/me` for the full view. **Starter packs row** at the bottom: 5 subscribe cards (Student / Office / Media / Vibe / Dev) with blurb + progress ("3 of 5 in plan"); click → confirm → `addPathwayItem(topicId, 'seed')` for each unowned topic; "All added" state when the user already has the whole bundle. Route `/learn/pathway` now 301s to `/me` (soft-retire via `<Navigate>`). `MyPathway.tsx` and `.module.css` deleted. `/learn/custom` link stays as a deep-end escape hatch. No onboarding / Settings changes this chunk — those come in K.
- **`/me` page — reflective surface (Chunk I, 2026-04-23)** — New route at `/me`, reachable from UserMenu via "My progress" (Gauge icon) above Settings. Two modules: (1) **Report card** — overall mastery bar + category grid using the new Category layer (Foundations / Building with AI / AI at Work / Generative Media), each card showing progress bar + letter grade from `mastery.ts` + "N of M completed · X mastered" caption; (2) **My plan** — the MyPathway list view migrated verbatim (reorder, archive, restore, prereq-gated add-topic) with **hard-delete-for-zero-progress** rule wired in — X button on a 0%-score item removes permanently; on any >0% item it archives (title tooltip distinguishes). **Emergent pathway label** in the page subtitle: "60% through Building with AI — your strongest focus so far" (computed by picking the category with the highest completed/total ratio among categories the user has actually touched). Default `UserProfile.handle` bumped from `undefined` to `'admin'` so fresh installs arrive signed in (pre-auth scaffolding). `MyPathway` page not yet retired — next chunk routes `/learn/pathway` → `/me`.
- **Obsidian-ready data foundations (Chunk H, 2026-04-23)** — Schema + seed + repo prepared for a tags-first, graph-ready content model. **New `Category` entity** (Dexie table v7) seeded with 4 buckets: Foundations / Building with AI / AI at Work / Generative Media. Every existing Track got a `categoryId` + `tags[]`. `Topic` gained `tags[]`, `relatedTopicIds[]` (symmetric see-also, distinct from `prereqTopicIds`), `relatedLibraryIds[]`. `LibraryItem` gained `relatedTopicIds[]` + `relatedLibraryIds[]`. `Project` gained `tags[]`, `relatedTopicIds[]` (broader than `gapTopicIds`), `relatedLibraryIds[]`. Taxonomy data lives in two seed maps (`TOPIC_TAGS`, `TOPIC_EDGES`) so it's grep-able and single-source-of-truth. seedIfEmpty gained a taxonomy pass that applies tags and writes edges bidirectionally — both sides of every edge land in storage so graph queries resolve without joins. **Repo helpers:** `listCategories`, `getCategory`, `listAllTags` (dedupe union across topics + library + tracks + projects), `nodesForTag` (every entity carrying a tag, returned typed per-kind), `getBacklinks` (inverse of every outbound edge across all entity types), `getNeighborhood(entityId, depth)` (BFS node-ID collector for future graph viz), `setRelated(a, b)` (symmetric edge writer — works topic↔topic, topic↔library, library↔library; idempotent). **Conventions locked:** tags lowercase, `/`-separated for eventual nested rendering (`prompting/patterns`, `tools/image`); `prereq*` = directed, `related*` = symmetric; bidirectionality enforced at the repo layer, not at the schema. 27 topics tagged (3–5 tags each), 10 topic↔topic edges authored, ~6 topic↔library edges authored. No UI changes this chunk — all additive. Unblocks Category rows on Learn, tag facet on Library, "Further reading" on topic pages, `/me` report card grouping, and the eventual graph view.
- **Quiz issue-reporting — Khan-style quiet flag (2026-04-22)** — Tiny "Report" flag in the lower-right of every question card and the result card, modeled on Khan Academy's "Report a problem" pattern (corner-tucked muted link) + Duolingo's canned-reason chips. Idle state: flag icon + "Report" label at 40% opacity, `var(--ink-3)` color; hover/focus raises to 100% + faint bg. Click → inline form (no modal) with four canned kinds (Wrong answer / Unclear wording / Typo / Other), optional 2-row textarea for detail, Cancel + Send. Submit → "Thanks — logged" confirmation fades in+out over 1.8s. Data flows through `repo.logQuizReport` to new Dexie table `quizReports` (schema v6, indexed by quizId/questionId/ts/resolved). `QuizReport` type carries `{id, quizId, questionId?, kind, note, ts, resolved?}` — questionId omitted when reporting the whole quiz (result-card flag). Admin triage page deferred; records are retrievable via `repo.listQuizReports()` for future admin surface / DB sync.
- **Pathway coverage closure — 14 new topics + template revisions (Chunk G, 2026-04-22)** — Closed every gap the pathway-coverage research flagged. Three new tracks: `literacy` (order 0, audience all), `office-ai` (order 6, audience office), `generative-media` (order 7, audience media). **14 new topics with full lessons + quizzes** (~85 quiz questions total): `t.ai-literacy` (hallucinations, trust calibration), `t.ai-literacy-at-work` (sign-your-name rule, what never to paste, Mata v. Avianca), `t.ai-for-students` (integrity lines, study moves that work), `t.models-compared` (Claude/ChatGPT/Gemini tier landscape), `t.tool-use` (authored lesson for an existing title-only topic), `t.agents-intro` (plan/act/observe loop, compounding errors), `t.prompt-caching` (prefix rule, 5-min TTL, classic cache-miss bugs), `t.claude-for-office` (Projects + custom-instructions coworker mindset), `t.docs-with-ai` (five doc-work shapes), `t.meetings-with-ai` (before/during/after phases), `t.generative-media-101` (rights + consent + disclosure), `t.image-generation` (Midjourney/Nano Banana/Ideogram/Flux), `t.video-generation` (Veo/Sora/Runway; shot prompting; stitching), `t.voice-and-audio` (ElevenLabs/Cartesia/Suno; consent). **Templates revised:** student + office now lead with literacy (not mechanics); dev adds `t.prompt-caching` and fixes the `t.streaming-ui → t.streaming` slug mismatch by demoting streaming to catalog; media leads with the 101 orientation topic; vibe unchanged. Every template slot now references a fully-authored topic — no more silent drops at stamp time. Research decision: **no 6th pathway** (data-analyst + everyday-AI cases covered via catalog sub-topics inside dev and office respectively; revisit on usage signal).
- **Content expansion — 5 new topics for the vibe pathway (Chunk F, 2026-04-22)** — New `vibe-coding` track (audience `['vibe']`, order 5). Five new topics + lessons + quizzes now exist so the revised vibe template references real content: `t.prompt-basics` (cross-pathway, lives in prompt-eng track, order 0), `t.vibe-what-and-why`, `t.vibe-tools-compared` (Cursor / Claude Code / v0 / Lovable / Replit / Supabase), `t.claude-code-basics`, `t.vibe-iteration-loop` (prereq chain links all four inside the track). Lesson bodies follow the seedLibraryNotes format (TL;DR → `##` sections → `>` inline citations with `— Source`), 7–10 minutes each. Quizzes exercise the new question kinds: `q.prompt-basics` has ordered-steps + short-answer; `q.vibe-what` has ordered-steps + short-answer; `q.claude-code` has TWO code-typing questions for the `/init` and `/clear` slash commands; `q.vibe-loop` has ordered-steps + short-answer. Total new: 1 track, 5 topics, 5 lessons, 5 quizzes (33 questions across them). **`seedIfEmpty` gained an insert-if-missing pass** for tracks/topics/lessons/quizzes on existing installs so users with pre-seeded DBs see the new content without needing to wipe storage. Chunk F content is now authored — Chunks D templates reference real topic IDs; no more silent-drop behavior in `seedPathwayFromTemplate`.
- **Vibe + Media project intakes, with YouTube creator flow (Chunk E, 2026-04-22)** — `ProjectNew.tsx` now dispatches to four pathway-specific intakes: office (existing), vibe (new), media (new), and the build-oriented default for dev/student/all. `ProjectNewVibe.tsx` = 4 steps (Title → One-sentence vision → Stack sketch with chip suggestions for Cursor / Claude Code / v0 / Lovable / Replit / Supabase + free-text textarea → Gap topics picker, sorted vibe-first). `ProjectNewMedia.tsx` = 4 steps (Title → Concept → Output kind → Tools). Kinds: **youtube (new, first in list), image, video (now "short video"), voice, audio, multi**. YouTube gets a creator-shaped checklist (hook/thesis → outline → record a/b-roll → edit to pace → thumbnail+title+desc+chapters → upload+schedule). Media tool chips include YouTube-creator picks (CapCut, DaVinci, Descript) surfaced as "manual" when not yet in inventory. New optional `Project.mediaKind` + `Project.stackNotes` (preserved free-text stack sketch). `ProjectNew.module.css` gained `.chipRow` + `.pickChip` + `.pickChipOn` + `.chipMuted` styles used by both new intakes. Office + build/dev flows untouched.
- **"My pathway" + vibe research pass (Chunk C + research, 2026-04-22)** — New Dexie table `userPathwayItems` (schema v5) with rows `{id: 'upi.<topicId>', topicId, status: 'active'|'archived', position, addedAt, source: 'seed'|'manual'|'project'}`. Repo methods: `listPathwayItems`, `hasAnyPathwayItems`, `addPathwayItem`, `archivePathwayItem`, `unarchivePathwayItem`, `deletePathwayItem`, `reorderPathwayItems`, `seedPathwayFromTemplate`, `resetPathway` + private `compactActivePositions` + `mergeProjectGapsIntoPathway` called from `repo.putProject` (non-blocking; failure logs and continues). Stamping respects history — `seedPathwayFromTemplate` no-ops if any rows exist. `pathway: 'all'` skips stamping. New `/learn/pathway` page (`src/pages/MyPathway.tsx`): up/down reorder, archive/restore, add-topic modal with prereq gating, reset-to-default button, empty state with "Use the X default / Build from scratch" CTAs. Learn homepage shows a top-3 "My pathway" panel replacing the old "Build a custom pathway" link position (the custom-pathway link remains below). Dashboard's "Next up" prefers the first non-mastered active pathway topic, falling back to the legacy catalog scan. Onboarding `finish()` and Settings pathway-switch both call `seedPathwayFromTemplate`. `pathwayTemplates.ts` holds the five starter templates; **vibe template revised per research** → `t.vibe-what-and-why → t.prompt-basics → t.vibe-tools-compared → t.claude-code-basics → t.vibe-iteration-loop` (these topics don't yet exist; Chunk F authors them, template tolerates missing IDs at stamp time). **Audience bug fix:** `deriveLibraryAudience` now routes `cli + agent` (and any `agent`-tagged tool) to `['vibe', 'dev']` instead of dev-only, surfacing Claude Code to the vibe pathway it was built for. Research output at `revamp/docs/research-vibe-pathway.md` (10 vibe-content gaps, 5-slot template justification, reuse-vs-author mapping per slot). STATE: Chunk D's "Reset pathway" button is already in MyPathway — Settings duplicate deferred.
- **Three new quiz question types (Chunk B, 2026-04-22)** — `QuizQuestion` is now a discriminated union on `kind`: `'mcq'` (default, legacy rows tolerated), `'ordered-steps'` (drag/reorder, exact-match scoring), `'code-typing'` (fill-in-the-blank with `{{blank}}` marker, whitespace-collapsed string match, optional `caseInsensitive`), `'short-answer'` (free text; `expected` for case-insensitive equality OR `pattern` for regex with flag `i`; `pattern` wins). New `src/lib/quizGrading.ts` centralizes `gradeQuestion` + `isAnswerable` + `questionKind`. `QuizView.tsx` rewritten around a `QuestionBody` dispatcher — pick/advance/end-only-feedback flow preserved, keyboard 1–9 still picks MCQ, Enter advances when answerable and input focus is not on a typable field. Ordered-steps rows are keyboard-reorderable (Up/Down on the focused step, plus explicit up/down buttons). Seeded examples: `q.tokens.5` ordered-steps (prompt → token pipeline), `q.tokens.6` code-typing (`max_tokens` JSON blank), `q.tokens.7` short-answer (regex `^tokens?$`). Existing four MCQ rows migrated to explicit `kind: 'mcq'`. Grading is scored in-UI and passed as a fraction to `repo.recordQuiz` (contract unchanged).
- **5-pathway expansion + audit cleanup (Chunk A, 2026-04-22)** — `Audience` is now `student | office | media | vibe | dev`, ordered by ascending code involvement. Labels: Student / Office / Media creator / Vibe coder / Developer. `deriveLibraryAudience` reclassified: IDE/framework → `vibe+dev`; image/video/voice/audio → `media+office`; automation → `office+vibe`; deep-eng tags (cli/sdk/orm/ssr/react/routing/language/state/build/devops/container/vcs) stay `dev`-only; chat/model/foundations → everyone. Onboarding + Settings work-style pathway tags updated (`no_code` → office+media; `vibe_code` → vibe). Seed track audiences extended. `PATHWAY_BLURBS` gained vibe + media entries. **Scenario-audit items closed:** #3 quiz-pass flash now reads dynamic bucket name (`Completed` or `Mastered`) via `MASTERY_LABEL[masteryStatus(score)]`; #4 lesson + topic done-chip unified to "Completed" (project-status "Done" left alone — Linear vocab); #5 enum verified already aligned post-grading-overhaul; #11 friendly "not found" fallbacks on LessonView + QuizView when `repo.getLesson`/`repo.getQuiz` return null (previously blank page).
- **Grading overhaul + quiz flow refinements** — 4 status buckets (Not started / In progress / Completed / Mastered); PASS_THRESHOLD=0.50, MASTERY_THRESHOLD=0.90, with 0.80 reserved as the "true understanding" bar for the future report card. Letter-grade (F/D/C/B/A/A+) and accolade-tier helpers in `src/lib/mastery.ts`. Retake now overwrites prior score (latest-attempt-wins, Khan-style). Quiz flow removed mid-question feedback — pick, advance, see result only at end. Zero-question quiz shows a friendly "unavailable" message instead of a blank page. Retake affordance surfaces as a chip on TopicDetail (not on the result screen — extra step by design).
- **Project intake ends with Save as plan / Start now** — dev + office intakes let the user pick initial status. Status stays user-controlled, no auto-promote; editable any direction.
- **Status colors + demo projects** — Backlog grey, Planned dim blue (mixed with white so warm canvas doesn't green-shift it), In progress accent, Completed green, Canceled red. Seed backfills 5 demo projects (one per status). Chip `muted` variant added.
- **Dashboard minimal** — Library panel dropped (Activity feed + nav cover it); Activity feed dropped (too busy); Dashboard eyebrow/title dropped; just the two panels (Learn, Projects) with condensed one-line recent rows. Empty states keep the panel shape (reserved 66px). Learn recents dedupe by topic (lesson + quiz touches on the same topic collapse to one row linking to topic detail).
- **Theme toggle moved to Settings → Appearance** — topbar reduced to brand + nav + search + account menu; account anchors top-right per convention.
- **Library kind icons** — Wrench / FileText / BookOpen / Film replace the single-letter T/D/R/V badges next to each library row.
- **Collapsible "Everything else"** — dev pathway gets the split but starts collapsed; student/office default expanded. `shouldCollapseRestByDefault` helper + new `ui/Disclosure` component.
- **Pathway sorts, never filters** — the pivot from hard filter to soft sort. All content is always visible; matching content floats to the top under a "For you" band, rest sits below. Dev + All get one merged list (before the collapsible update, above). New helpers: `isPrimaryForPathway`, `splitByPathway`, `audienceBadge`. Inline `AudienceBadge` pill on every track / library row.
- **Pathway selector moved into UserMenu dropdown** — freed topbar density on phones. Radio-style group at the top of the menu; Settings + Retake onboarding below.
- **Topbar reorder** — left cluster is brand + user menu; desktop nav sits middle; right cluster is search + theme toggle.
- **UserMenu dropdown** in the topbar — `@handle` / "Guest" label, opens to pathway radio, Settings link, Retake onboarding. Outside-click / Esc close.
- **Settings page** at `/settings` — full profile edit surface (handle, pathway, work styles, devices, years coding [dev only], known topics, reset). Inline persistence note that settings are local-only until auth lands.
- **Sign-in preview** at `/signin` — design-only shell for the 2FA flow (sign in · sign up · sent · TOTP enroll · TOTP verify · recovery codes · recovery entry). Not linked; preview state picker at the bottom. No real auth.
- **Onboarding flow** at `/onboarding` — first-visit redirect (skippable); 6-or-7-step optional profile setup (welcome → pathway → work styles → devices → [years coding for dev only] → known topics → done). Dismissed flag in userStore persists the "don't re-redirect" decision.
- **Extended user profile** — `UserProfile` with `handle`, `workStyles[]`, `devices[]`, `yearsCoding`, `knownTopicIds[]` in zustand (localStorage-persisted, version 2); matching columns drafted in Supabase schema (`user_profile`). Tag-based vocab for work styles; permissive `text[]` on DB side so new values don't need a migration.
- **Library filter popover + topic mastery breakdown** — collapsed facets + sort into a Filter button with active-filter clear chips; Khan-style "N/M lessons · Q/R quizzes mastered" line under topic mastery bar.
- **Per-project status-change log** — `projectEvents` Dexie table (v4), auto-logged on `repo.putProject` diff; History section on ProjectDetail with from→to chips + relative timestamps.
- **Global search** — Ctrl/Cmd+K modal indexing tracks, topics, lessons, projects, library; results ranked so pathway matches come first, no content hidden. Keyboard nav + Esc close.
- **Cross-surface activity feed** on Dashboard — last 8 events across Learn + Projects + Library.
- **Custom pathway builder** at `/learn/custom` — user picks topics, Kahn topo-sort orders them by prerequisite; topics gained `prereqTopicIds`.
- **Office-pathway project intake** — 4-step workflow-oriented flow (document / meeting / announcement / analysis / cadence) when active pathway is `office`; `ProjectNew` dispatches.
- **Audience tagging (3 pathways)** — `Audience = student | office | dev` on Track/Topic/LibraryItem; library items auto-derive audience at seed time from kind / tags / category.
- **Library search-miss logging + wishlist admin** — `searchMisses` Dexie table (v3), debounced log on empty results, `/library/wishlist` triage page.
- **Electrified progress bars** — thin glowing line grows left-to-right, optional milestone nodes light on pass, IntersectionObserver re-plays on scroll-back, single-motion rule.
- **Learn + Projects section-homepages** — rollup cards above each list with continue / resume CTAs.
- **Dark mode (v1)** — `[data-theme="dark"]` in tokens.css, topbar toggle, no-flash init. Polish pass deferred until light-mode is finalized.
- Vite + React + TS + Dexie + Framer Motion + Zustand + React Router + highlight.js baseline.

## Remaining work (prioritized)

### Active (next up) — ATOM TRANSITION-MATH ITERATION

**Lab redesign shipped 2026-04-25.** The 7-chunk tracker in [`revamp/docs/atom-system-plan.md`](docs/atom-system-plan.md) is fully complete:
- `/labs/atom-states` — single-state isolation lab (orbit / straight / spiral / pulsate / pause), per-state controls, color picker, Replay, HUD.
- `/labs/atom-transitions` — two-state sequence with composition-rule validation, `transitionWindow` slider, A/B duration knobs, Replay, boundary-chip readout (measured Δ|v|/peakV%, green/yellow/red), HUD.
- `AtomLabHud` primitive — bottom-pinned 4-line monospace, 30Hz math via direct DOM textContent, hidden `data-atom-debug-context` JSON node, 1s `console.debug` heartbeat.
- `import.meta.env.VITE_GIT_COMMIT` injected at build via vite.config `define` (verified empirically).
- Runtime modules under `revamp/src/ui/atom/runtime/` — `positionFn`/`scaleFn` per state + `evalState()` + `evalVelocityMagnitude()` + `defaultConfigFor()` + composition guard + `computeWindowMs()`.
- `/labs/atom-blend-test` retired; dt-floor (1/240) + 3-frame velWarmup salvaged into the new transitions lab.
- `usePrefersReducedMotion()` hook gates both labs to a static end-state when set; HUD still renders.

**Seam-blending math shipped 2026-04-25** — the locked plan's per-boundary table (smoothstep on curve↔curve, Hermite on curve↔straight, fillet arc on straight↔straight, ramp from/to zero on pause edges) collapsed to a single universal solution: **Hermite cubic across the window using each state's analytical tangent**, sampled via central finite-difference of `evalState`. C1 continuity at u=0 and u=1 is mathematically guaranteed (verified algebraically — see `runtime/transitions.ts` header). Edge cases handled by construction: pause/pulsate yield zero tangent → cubic ramps from/to zero; straight↔straight at angle gets a corner fillet for free; identical A=B reduces to identity. Speed shaping is implicit in the cubic's |dP/du| profile.

**Next workstream:**
1. **Phone-validate** — open `/labs/atom-transitions`, sweep through all legal A/B pairs, drag `transitionWindow` from 0→1, confirm boundary chips read green (Δ|v|/peakV < 2.5%) at every pair.
2. **Refine if anomalies surface** — domain-edge tangents may need clamping (the finite-diff probe can hit t<0 or t>1); revisit if any pair shows unexpected red.
3. **First preset deployment** — port the system to a real consumer (likely quiz reward or Genius A+ celebration). Pick declarative-vs-imperative trigger API at that point (currently deferred).
4. **Color system** — still deferred; design when first preset needs it.

**Locked decisions (do NOT re-ask):** 5 states (orbit/straight/spiral/pulsate/pause); composition rules (spiral.in must follow orbit; spiral.out must follow at-point); `transitionWindow` knob with formula `windowMs = transitionWindow · 0.5 · min(durLeft, durRight)`, MIN_WINDOW_MS=40ms; dual `positionFn` + `scaleFn` runtime; `TargetSpec = { space, value }` (deferred to consumer trigger API); trail invariant (autonomous, fade doesn't touch); HUD spec (4 lines, bottom-pinned, 30Hz math via direct DOM write).

**Cold-start read-order for next session:**
1. `revamp/docs/atom-system-plan.md` (locked plan + chunk tracker — fully checked off)
2. `revamp/src/ui/atom/runtime/{types,orbit,straight,spiral,pulsate,pause,index,transitions}.ts` (state runtimes + composition guard + windowMs formula)
3. `revamp/src/pages/LabsAtomTransitions.tsx` (the stage on which seam math gets iterated; concat-only today)
4. `revamp/src/ui/atom/AtomLabHud.tsx` (HUD primitive both labs share)
5. `.claude/skills/electron-motion/SKILL.md` if any motion question arises

**Other Active items (lower priority than atom rework):**
- [ ] Extend Learn — more tracks + topics + authored lessons. Pending.
- [ ] **Tool-overview courses** — full roster (Slack, Teams, Zoom, GitHub, Miro, Service Cloud, ServiceNow, Zendesk, MS Office suite). One smoke test shipped earlier; remainder pending.

### Planned (later)

- [ ] **Topic: `t.citing-sources-with-ai` (Literacy track, all pathways).** Author a dedicated lesson + quiz on the editorial policy codified in `/colophon`. Covers: why citation matters (legal + ethical), how short quotes differ from copied chunks, how to structure a Sources footer, how to attribute AI-assisted content honestly, what happens when you don't cite (Mata v. Avianca-style incidents). Reference the Colophon's Editorial policy directly from the lesson. Should include a practical segment on applying the rule to user-authored Project descriptions and future user-contributed content. Target audience: all 5 pathways (literacy-first, ethics-adjacent).


- [ ] **Freshness Pipeline (post-Claude-API + post-DB + post-admin).** The app's strategic differentiator: **content freshness**. Stitches together three already-parked items under one loop so their design decisions stay coherent:
  1. **Scraper layer** — revive/repoint the legacy-root `update-feed` pipeline (`data/latest.json`, runs every 2h) so newly-indexed sources flow into an admin queue.
  2. **Claude drafts layer** — once the Claude API project lands, Claude reads scraped material + catalog taxonomy and proposes new topics / lessons / quizzes / library items in the seed-compatible shape.
  3. **Admin review layer** — once the admin role is wired (post-DB), the admin gets a review queue: approve, edit, reject each draft. Approved drafts publish with a "new" flag that fades after N days.
  The value this produces is "the app's content is always current" — differentiates from the snapshot-in-time quality of static learning apps. Decisions today (topic/library schema, admin flags, seed-compatible shape for drafts) should preserve a clean path into this loop. **Do NOT build pieces in isolation before the predecessor pieces are ready** — order is DB migration → admin role → Claude API integration → Freshness Pipeline.
- [ ] **AI-generated custom pathway (post-Claude-API project).** Once the separate Claude API integration lands, add a "Describe yourself" input that accepts a natural-language prompt: user's background, goals, worries, what they already know. Claude reads this + the full topic catalog (id, title, summary, tags, category, prereqs) + known-topics + emergent-pathway signals from `/me` and returns an ordered `string[]` of topic IDs. Feed straight into `repo.addPathwayItem` for each — no new plumbing, the data shape is already right. Fits alongside the existing three add-paths: engagement prompts, starter packs, manual pick. **Do NOT build before the Claude API project ships** — this depends on it.


- [ ] **DB migration — Supabase (Postgres + auth + RLS + TOTP MFA)**. ~1 month out. Schema drafted in `revamp/docs/supabase-schema.sql` + visualized in `revamp/docs/supabase-schema.html`. Fresh-start model: no IndexedDB → Supabase import, every user starts clean (users have been warned). Multi-user from day one (~5 people), RLS-isolated; friend-view scaffolding (visibility column + friendships table) is in the schema but deferred. Signup gating via `signup_allowlist` table + BEFORE INSERT trigger on `auth.users`. Decisions locked: handle + citext for public URLs, UUID for new user-minted IDs (legacy `p.xxx` still accepted), RESTRICT cascades on content tables, TOTP MFA with 7-day grace. `/signin` preview UI wires up here.
- [ ] *(First wave being handled inside the Active autonomous run — Chunk D.)* **Predefined pathway templates (specialty packs)**. Pathway (student/office/dev) is coarse — real personalization wants the sub-role. Examples: Office + Marketing vs. Office + Finance each want different Library tools, different Learn recommendations, different project intakes. `user_profile.workStyles[]` already carries the sub-role shape; extend the canonical list (add `marketing`, `finance`, `sales`, `ops`, `hr`, `legal` for Office) and let admins define named **pathway templates** that bundle `(pathway, workStyles, known_topic_ids, recommended library item ids, initial topic picks)`. Onboarding asks "what role / team are you on?" after the coarse pathway pick, then **seeds the first version of "my pathway" (see next item)** — a starting point, not a finished plan. Templates live in the DB (admin-authored) so new specialties don't require a deploy. *Not in conflict with the "persona switcher deferred" item below — those are a runtime flip UI; these are an onboarding seed.*
- [ ] *(First wave being handled inside the Active autonomous run — Chunk C.)* **"My pathway" — persistent, editable user learning plan.** Builds on the existing Custom Pathway Builder (`/learn/custom`), which today is ephemeral. Shape:
  - **Seed.** Onboarding's predefined template (above) stamps the first version — an ordered list of topics the user is planning to take.
  - **Interactive build.** User adjusts the seed immediately: add topics, remove topics, reorder within the prereq constraints. Finalize to save.
  - **Editable forever.** Open "my pathway" later → add things that came up, remove things they no longer want to take, mark as done.
  - **Soft-delete, never destructive.** Removing a topic from "my pathway" is a hide / archive action — never a hard delete. The pathway row gets a status (`active | archived`), archived rows live behind a "Show archived" disclosure so the user can see their full learning record and re-add anything anytime. *Progress + mastery records are already decoupled* (they key off topicId, not pathway membership), so pathway edits never wipe quiz scores or completion history regardless — the archive flag is purely a view filter on the pathway itself.
  - **Persistence.** Today (Dexie): new per-user table holding pathway rows (topicId, status, position, addedAt). Post-DB: `user_pathway_items` table with `user_id` + `topic_id` + `status` + `position` + `created_at`, RLS-owned.
  - **Surfaces.** Entry point on the Learn section-homepage (replaces/merges with "Build a custom pathway" link); maybe a rollup tile on Dashboard.
  - The *visual tree/line/cluster* representation of the pathway is its own Deferred item below — this Planned item is the data + list-style UI.
- [ ] **Admin role + admin-only surfaces** (post-DB, owner-only). `user_profile.is_admin` (already in the schema draft) gates everything here. Never user-editable via RLS. **YouTube curation is entirely admin-only — public users only ever see videos the admin has explicitly pinned and organized.** Scope:
  - **YouTube curation + in-app authoring queue.** Admin has a browsing / feed surface (see the YouTube API line below) to triage videos. The **pin action opens a required form** prompting for: target (topic / track / tool / library item), tags, and notes. Alternative action: **add to wishlist** to decide later. Only pinned videos land in the public Library; the wishlist + authoring queue stay admin-side. This is the concrete realization of the general "in-app authoring flow" idea — not a separate feature.
  - **Dev debug panel.** Feature flags, fake-user switches, DB dump, reset buttons.
  - **Live theme tweaker.** Color picker that updates `--accent-base` / `--danger-base` / `--mastery-base` in the running app so design iteration doesn't require a deploy.
- [ ] **YouTube API integration** — powers the admin-only browse / feed surface. Admin can search YouTube and see recent uploads from tracked channels without leaving the app, then trigger the pin-form or wishlist flow above. Needs interactive Google OAuth. Deferred until the manual video-ID-paste path in the admin pipeline is live and we feel the friction. Never exposed to non-admin users.
- [ ] **User personalization controls** (not content authoring — just tweaks to how the user sees the app). In scope: per-item notes, reorder / hide-from-view elements on a page, user-owned pathways (custom pathways are already shipped), bookmarks. Admin-authored content remains the canonical source; these are overlays on top of it, stored per user. Touch later.
- [ ] Project bootstrapper — scaffold files + run init commands from a project's stack pick.
- [ ] Resume / public project-detail pages (the "site IS the resume" north star).

### Deferred (explicitly parked)

- Read-only friend-view — user explicitly deprioritized. Schema scaffolding is already in place (`visibility` column on `user_projects`, `friendships` table) but no UI or cross-user RLS until the user opts back in.
- **Visual pathway map — tree / line / cluster diagram of "my pathway."** The aspirational visual for the item above: an abstract chart that arranges the user's plan spatially (topics clustered by concept, a timeline/line, or a tree with branches) rather than as a vertical list. Fights the Linear-minimal aesthetic and is a large build; parked as a future want. (Previously tracked here as "electrified-circuit skill-map viz.")
- Inventory dedicated page — schema ready, UI deferred.
- Persona switcher — out of scope per user decision. A runtime picker to flip between "personas" is different from onboarding pathway templates (still Planned above); this line refers only to the former.
- Claude API integration — **separate future project**, not a step in this build. Never propose as next-up.
- User-authored content (lessons, quizzes, etc.) — depends on the separate Claude API project to handle the complex authoring assistance. Even once it lands, every user-authored piece goes through admin review before it's published. Out of scope for this build; captured so the path is known.

## How to preview locally

```bash
cd revamp
npm run dev
```
Opens http://localhost:5173

For phone testing on the LAN:
```bash
npm run dev -- --host
```
and open `http://<your-lan-ip>:5173/`.

## Deploy

Every push to `main` touching `revamp/**` auto-deploys via GitHub Actions.

## Key file paths

- `src/styles/tokens.css` — design tokens (single source, light + `[data-theme="dark"]`)
- `src/app/App.tsx` — routes; first-run onboarding redirect; out-of-shell renders for `/onboarding` and `/signin`
- `src/app/AppShell.tsx` — topbar (brand + UserMenu | nav | search + ThemeToggle), bottom nav (mobile), Cmd/Ctrl+K shortcut
- `src/db/{types,schema,repo,seed,toolBodies,seedLibraryNotes}.ts` — data layer
- `src/pages/*` — Dashboard, Learn, CustomPathway, TopicDetail, LessonView, QuizView, Projects, ProjectNew (dispatcher) + ProjectNewOffice, ProjectDetail, Library, LibraryDetail, LibraryWishlist, Onboarding, Settings, SignIn
- `src/ui/*` — PageHeader, Button, Chip, ProgressBar (electrified), Tile, List, Row, Markdown, grid (class-name consts), ThemeToggle, GlobalSearch, UserMenu, AudienceBadge, Disclosure
- `src/lib/projectRoutes.ts` — Easiest / Cheapest / Best logic
- `src/lib/projectStatus.ts` — Linear-style status vocabulary + legacy-value migration
- `src/lib/audience.ts` — pathway types (`UserPathway`, `Audience`) + `matchesPathway` (intake-only hard filter) + `isPrimaryForPathway` + `splitByPathway` + `shouldCollapseRestByDefault` + `audienceBadge` + `deriveLibraryAudience` + `AUDIENCE_LABEL`
- `src/lib/pathwayOrder.ts` — Kahn topo-sort for custom-pathway builder
- `src/lib/activity.ts` — unified event stream + `whenShort` relative-time helper
- `src/lib/useInViewReplay.ts` — IntersectionObserver hook for re-triggering animations
- `src/state/userStore.ts` — zustand (with persist, v2): full `UserProfile` — handle, pathway, workStyles[], devices[], yearsCoding, knownTopicIds[], onboardingSeen; actions for each
- `docs/supabase-schema.sql` — Postgres schema draft for the planned DB migration (includes `user_profile.is_admin`, `signup_allowlist`, `friendships` scaffold)
- `docs/supabase-schema.html` — schema visualization (Mermaid ER + table cards)
